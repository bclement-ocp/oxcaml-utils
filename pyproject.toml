[project]
name = "oxcaml-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.40.1",
    "pyarrow>=25.0.0",
]

[dependency-groups]
dev = [
    "ipython>=9.13.0",
]
